name: Count Files
on:
  push:
    branches:
      - fleet-temp-1776005778011
jobs:
  count:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repo and count files
        run: |
          git clone https://github.com/k0inwork/test.git repo
          cd repo
          find . -type f | wc -l
